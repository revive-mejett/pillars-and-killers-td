type SlowDebuffStats = {
    speedMultiplier: number,
    timeLeft: number
}

type VulnerableDebuffStats = {
    extraDamage: number,
    timeLeft: number
}

export { SlowDebuffStats, VulnerableDebuffStats }