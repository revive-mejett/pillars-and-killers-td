type TowerStats = {
    range : number,
    damage: number,
    fireRate: number,
    cost: number,
    level: number,
    asset: PIXI.SpriteSource
    assetIcon: PIXI.SpriteSource
}

export default TowerStats